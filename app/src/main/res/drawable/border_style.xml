<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">  <!-- Forme rectangulaire -->


    <stroke android:width="2dp"
        android:color="@color/white"/>

    <!-- Arrondir les coins si désiré -->
    <corners android:radius="8dp" />

    <!-- Ajouter un padding autour du texte dans l'EditText -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp" />
</shape>