<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    >  <!-- Forme rectangulaire -->

    <stroke android:width="1dp"
        android:color="@color/deliveryContainer"
        />

    <!-- Arrondir les coins si désiré -->
    <corners android:radius="2dp"/>
     <solid android:color="#F3F3F3" />
    <!-- Ajouter un padding autour du texte dans l'EditText -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp" />


</shape>